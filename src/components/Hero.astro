<section class="relative h-screen flex items-center px-6 lg:px-32 overflow-hidden">

    <!-- VIDEO BACKGROUND -->
    <div class="absolute inset-0 z-0 overflow-hidden bg-brand-black bg-center bg-cover">
        <video
        id="hero-video"
        class="absolute inset-0 w-full h-full object-cover opacity-0 transition-opacity duration-[1200ms] hidden md:block"
        autoplay muted loop playsinline preload="auto"
        poster="/hero-poster.webp"
        >
        <source src="/hero-bg.mp4" type="video/mp4" />
        </video>
        <!-- DIAGONAL LINES -->
        <div class="absolute -right-[8%] top-[5%] w-1/2 h-[90%] opacity-[0.04] z-[2] bg-[repeating-linear-gradient(-25deg,transparent,transparent_50px,#e8872b_50px,#e8872b_51px)]"></div>

        <!-- TRIANGLE MOTIF -->
        <div class="absolute right-[8%] top-1/2 -translate-y-1/2 w-[420px] h-[420px] opacity-[0.08] z-[2] hidden lg:block">
            <div class="absolute inset-0 [clip-path:polygon(50%_0%,0%_100%,100%_100%)] bg-gradient-to-b from-brand-orange to-brand-orange/30"></div>
            <div class="absolute inset-[30px] [clip-path:polygon(50%_0%,0%_100%,100%_100%)] border-2 border-brand-orange opacity-50"></div>
        </div>

        <!-- CONTENT -->
        <div class="relative z-10 max-w-130 pt-24 pb-16 lg:pt-0 lg:pb-0 flex flex-col justify-center h-full ml-25">

            <div class="font-mono text-[0.72rem] text-brand-orange tracking-[4px] uppercase mb-6 flex items-center gap-3 animate-fade-up [animation-delay:0.2s]">
            <span class="w-9 h-0.5 bg-brand-orange"></span>
            Rijeka, Croatia · Est. 1996
            </div>

            <h1 class="text-4xl md:text-5xl font-heading lg:text-[4.4rem] font-black leading-[1.05] -tracking-wide mb-6 animate-fade-up [animation-delay:0.4s]">
            Marine & Industrial<br />
            <em class="not-italic text-brand-orange">Automation</em> Solutions
            </h1>

            <p class="text-base md:text-lg text-brand-gray leading-relaxed max-w-[540px] mb-10 animate-fade-up [animation-delay:0.6s]">
            We design, build, commission and maintain control systems for
            vessels and industrial facilities — delivering competitive edge
            to customers worldwide.
            </p>

            <div class="flex flex-wrap gap-4 animate-fade-up [animation-delay:0.8s]">
            <a href="#services"
                class="bg-brand-orange text-brand-black px-8 py-4 font-bold text-sm tracking-wider uppercase hover:bg-brand-orange-bright hover:-translate-y-0.5 hover:shadow-[0_10px_35px_rgba(232,135,43,0.35)] transition-all inline-flex items-center gap-2">
                Our Services →
            </a>
            <a href="#projects"
                class="border border-brand-gray-dark text-brand-white px-8 py-4 font-semibold text-sm tracking-wider uppercase hover:border-brand-orange hover:text-brand-orange transition-all">
                View Projects
            </a>
        </div>
    </div>
    </div>
    <!-- STATS — flows below buttons on mobile, positions to bottom-right on lg -->
    <div class="relative z-10 flex gap-8 animate-fade-up [animation-delay:1s]
                lg:absolute lg:bottom-16 lg:right-32 lg:mt-0 lg:gap-14 mb-25">
        <div class="text-center">
            <div class="font-mono text-3xl md:text-4xl font-bold text-brand-orange leading-none">25+</div>
            <div class="text-[0.65rem] text-brand-gray uppercase tracking-[2.5px] mt-2">Years</div>
        </div>
        <div class="text-center">
            <div class="font-mono text-3xl md:text-4xl font-bold text-brand-orange leading-none">50+</div>
            <div class="text-[0.65rem] text-brand-gray uppercase tracking-[2.5px] mt-2">Projects</div>
        </div>
        <div class="text-center">
            <div class="font-mono text-3xl md:text-4xl font-bold text-brand-orange leading-none">E0</div>
            <div class="text-[0.65rem] text-brand-gray uppercase tracking-[2.5px] mt-2">Class Ready</div>
        </div>
    </div>

</section>
    <!-- Video fade-in script -->
	<script>
	  const video = document.getElementById('hero-video') as HTMLVideoElement;
	  if (video) {
	    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
	    if (prefersReducedMotion) {
	      video.pause();
	      video.style.display = 'none';
	    } else {
	      video.addEventListener('canplaythrough', () => video.classList.add('opacity-30'));
	      if (video.readyState >= 4) video.classList.add('opacity-30');

	      const observer = new IntersectionObserver((entries) => {
	        entries.forEach(e => e.isIntersecting ? video.play().catch(() => {}) : video.pause());
	      }, { threshold: 0.1 });
	      observer.observe(video.closest('section')!);
	    }
	  }
	</script>